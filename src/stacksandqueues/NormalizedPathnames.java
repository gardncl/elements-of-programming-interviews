package stacksandqueues;

public class NormalizedPathnames {

    /*
    9.4

    Write a program which takes a pathname, and returns
    the shortest equivalent pathname. Assume individual
    directories and files hav enames that use only
    alphanumeric characters. Subdirectory names may be
    combined using forward slashes (/), the current
    directory (.), and the parent directory (..).
     */

    public static String shortestEquivalentPath(String path) {

        return "";
    }
}
