public class ImplementFastSorting {

    /*
    14.9

    Implement a routine which sorts lists efficiently.
    It should be a stable sort, i.e., the relative
    positions of equal elements must remain unchanged.
     */

    public static ListNode<Integer> sort(final ListNode<Integer> L) {

        return new ListNode<>();
    }
}
